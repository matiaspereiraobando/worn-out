{
  "name": "worn-out",
  "version": "0.1.0",
  "description": "Worn Out — a jam game about planned obsolescence. Vertical slice.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.0.0"
  }
}
